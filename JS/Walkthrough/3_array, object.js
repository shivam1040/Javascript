 let arr=[1, 's', 3]; //arr can be initiallized by new Array(1, 2);
 let arr1=[1, 's', 3];
 Array.isArray(arr); //returns if arr is array or not
 arr.indexOf(1); //returns index of 1 in arr
 arr.push(55); //pushes 55 to end of array
 arr.unshift(66); //puts 66 at the front
 arr.pop(); //removes element from end
 arr.shift(); //removes element from start
 arr.splice(1, 2); //start from index 1 and remove two elements
 arr.reverse(); //reverses the array
 arr.concat(arr1); //concaat two arrays

 let obj={ //obj can have any datatype
     a:1,
     b:2,
     c:3
 }
console.log(obj.a);//obj[a] can be used tot, to access


