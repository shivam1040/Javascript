//if else usage same as javabn, to check for undefined, use typeof var!=='undefined'
//===, triple equal to checks both type and value. !== means value and type aren't matching
//one line conditional statements can be used too
switch(key){ //same as java
    case 1:
        break;
    case 2:
        break;
    deafult:
        break;
}